# Hybrid: 4 standard + 4 sparse
# Standard blocks first for global context, then sparse for efficiency

model:
  dim: 512
  num_heads: 8
  
  # 4 standard -> 4 sparse
  n_standard_blocks: 4
  n_sparse_blocks: 4
  num_hyper_nodes: 4

training:
  wandb_run_name: "hybrid-4std-4sparse-k4"

